import { ShoppingCart } from 'lucide-react';

function CartWidget() {
    return (
        <button className="btn">
            <ShoppingCart />
        </button>
    );
}

export default CartWidget;

